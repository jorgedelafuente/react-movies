{
   "mcpServers": {
      "playwright": {
         "command": "npx",
         "args": [
            "@playwright/mcp@latest",
            "--browser",
            "chromium",
            "--allowed-hosts",
            "localhost,127.0.0.1"
         ]
      }
   }
}
